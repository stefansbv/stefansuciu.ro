box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.69.2"
        theme: lanyon-hugo
        flags: --buildDrafts=false
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - pahnev/gh-pages@0.3.1:
        token: $GIT_TOKEN
        domain: stefansuciu.ro
        basedir: public

