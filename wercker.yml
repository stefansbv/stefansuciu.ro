box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.13
        theme: lanyon-hugo
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: stefansuciu.ro
        basedir: public
